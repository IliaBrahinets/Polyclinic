@model PatientRecord
@{}

<div class="container mt-2">
    <div class="row justify-content-center text-center">
        <div>
            <div>
                <a asp-action="GetPatientRecordAsWord" asp-route-id="@Model.Id">Скачать .docx</a> |
                <a href="javascript:frames[0].print()">Печать</a>
            </div>
            <iframe id ="Talon" src="GetPatientRecordAsHtml?id=@Model.Id">
            </iframe>
        </div>
    </div>
</div>
@section Styles{ 
    <link rel="stylesheet" href="~/css/Registrator/TalonView/main.css" />
}